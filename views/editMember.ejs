<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Edit Team Member</title>

  <!-- Tailwind CSS -->
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 text-white min-h-screen flex items-center justify-center">

  <div class="bg-gray-800 rounded-2xl shadow-lg p-8 w-full max-w-lg">
    <h2 class="text-3xl font-bold text-center text-orange-400 mb-6">
      Edit Team Member
    </h2>

    <form action="/team/member/update/<%= member._id %>" method="post" enctype="multipart/form-data" class="space-y-5">
      
      <!-- Name -->
      <div>
        <label class="block mb-1 font-semibold">Full Name</label>
        <input type="text" name="name" value="<%= member.name %>"
          class="w-full px-4 py-2 rounded-lg bg-gray-700 border border-gray-600 focus:ring-2 focus:ring-orange-400 focus:outline-none" />
      </div>

      <!-- Title -->
      <div>
        <label class="block mb-1 font-semibold">Title / Designation</label>
        <input type="text" name="title" value="<%= member.title %>"
          class="w-full px-4 py-2 rounded-lg bg-gray-700 border border-gray-600 focus:ring-2 focus:ring-orange-400 focus:outline-none" />
      </div>

      <!-- Site Name -->
      <div>
        <label class="block mb-1 font-semibold">Site Name</label>
        <input type="text" name="sName" value="<%= member.sName %>"
          class="w-full px-4 py-2 rounded-lg bg-gray-700 border border-gray-600 focus:ring-2 focus:ring-orange-400 focus:outline-none" />
      </div>

      <!-- Experience -->
      <div>
        <label class="block mb-1 font-semibold">Experience</label>
        <input type="text" name="experience" value="<%= member.experience %>"
          class="w-full px-4 py-2 rounded-lg bg-gray-700 border border-gray-600 focus:ring-2 focus:ring-orange-400 focus:outline-none" />
      </div>

      <!-- Facebook -->
      <div>
        <label class="block mb-1 font-semibold">Facebook URL</label>
        <input type="url" name="facbook" value="<%= member.facbook %>"
          class="w-full px-4 py-2 rounded-lg bg-gray-700 border border-gray-600 focus:ring-2 focus:ring-orange-400 focus:outline-none" />
      </div>

      <!-- WhatsApp -->
      <div>
        <label class="block mb-1 font-semibold">WhatsApp Number</label>
        <input type="text" name="whatsapp" value="<%= member.whatsapp %>"
          class="w-full px-4 py-2 rounded-lg bg-gray-700 border border-gray-600 focus:ring-2 focus:ring-orange-400 focus:outline-none" />
      </div>

      <!-- Messenger -->
      <div>
        <label class="block mb-1 font-semibold">Messenger Link</label>
        <input type="url" name="massenger" value="<%= member.massenger %>"
          class="w-full px-4 py-2 rounded-lg bg-gray-700 border border-gray-600 focus:ring-2 focus:ring-orange-400 focus:outline-none" />
      </div>

      <!-- Current Image Preview -->
      <% if (member.img) { %>
      <div class="text-center">
        <p class="font-semibold mb-2">Current Image</p>
        <img src="<%= member.img %>" alt="Profile Image" class="w-24 h-24 rounded-full mx-auto object-cover border-2 border-orange-400">
      </div>
      <% } %>

      <!-- Image Upload -->
      <div>
        <label class="block mb-1 font-semibold">Change Profile Image</label>
        <input type="file" name="img" accept="image/*"
          class="w-full text-sm text-gray-300 border border-gray-600 rounded-lg cursor-pointer bg-gray-700 focus:outline-none file:bg-orange-500 file:text-white file:px-3 file:py-2 hover:file:bg-orange-600" />
      </div>

      <!-- Buttons -->
      <div class="flex space-x-3 pt-4">
        <button type="submit"
          class="flex-1 bg-orange-500 hover:bg-orange-600 transition duration-300 py-2 rounded-lg font-semibold text-lg">
          Update Member
        </button>
        <a href="/team/member/create" class="flex-1 bg-gray-600 hover:bg-gray-700 transition duration-300 py-2 rounded-lg text-center font-semibold text-lg">
          Cancel
        </a>
      </div>
    </form>
  </div>

</body>
</html>
